{
  "name": "siphon",
  "private": true,
  "type": "module",
  "description": "Tooling only. The app in web/ has no build step and no dependencies — this exists so the in-browser extractor's pure logic can be tested with node --test. Tests live outside web/ because the Pages workflow publishes that whole directory.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "test:e2e": "node tests/e2e.mjs",
    "test:deployed": "node tests/deployed.mjs"
  }
}
